// import { btGateway } from '../../../config/bt-config';
// import { IClientToken } from './client-token.interface';

// const getToken = async (): Promise<IClientToken | null> => {
// 	const token = (await btGateway.clientToken.generate({}))?.clientToken;

// 	return { token };
// };

export const clientTokenService = {
	// getToken,
};
