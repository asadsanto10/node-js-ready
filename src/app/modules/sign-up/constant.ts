export const { DEFAULT_PRICE } = process.env;
export const { API_PROMO_CODE } = process.env;
export const { RECAPTCHA_SECRET } = process.env;
export const recaptcVerifyURL = `https://www.google.com/recaptcha/api/siteverify`;
